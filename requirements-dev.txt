# Semgrep pins OpenTelemetry ~1.37; installing it in the same venv as the app breaks resolution.
# Use: pipx install semgrep   OR   python -m venv .venv-semgrep && .venv-semgrep/bin/pip install -r requirements-dev.txt
semgrep==1.156.0
